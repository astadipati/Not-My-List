import React, {Component} from 'react';
import {Text, View} from 'react-native';

export default App = ()=> {
  return
    ( //es6 arrow function yang mereturn view
    <View style={{flex:1, justifyContent:'center', alignItems:'center'}}>
      <Text>Hello Ini dari Functional</Text>
    </View>
  );
}